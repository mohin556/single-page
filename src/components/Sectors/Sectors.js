import React from 'react';
import './Sectors.css'
const Sectors = ({sector}) => {
console.log(sector)
  const   {title, one, three, two} = sector;
    return (
      <div>
           
      </div>
    );
};

export default Sectors;